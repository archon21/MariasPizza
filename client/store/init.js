const defaultState = {
  phone: '(860) 267-8722',
  company: 'Marias Pizza'
};

export default function(state = defaultState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
